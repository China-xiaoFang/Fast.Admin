# 多租户与后台安全审查报告

审查范围：`Api.Server/src` 与 `Web.Admin/src` 的认证、授权、租户隔离、账号状态和动态路由链路。

## 已修复

| 等级 | 影响范围 | 根因 | 修复方案 | 验证方式 |
| --- | --- | --- | --- | --- |
| 高 | 任意租户用户身份 | `/tryLogin` 匿名且仅按可枚举的 `UserKey` 查询用户 | 要求认证，并强制目标用户的账号与当前账号一致 | 使用其他账号的 `UserKey` 调用接口应返回未授权 |
| 高 | 租户文件下载 | 下载查询显式清除租户过滤器 | 使用默认租户查询过滤器 | 租户 A 下载租户 B 的 `FileId` 应返回文件不存在 |
| 中 | 菜单、标签页与动态页面 | 路由组件解析返回字符串数组，且菜单可见性语义反转 | 返回动态导入函数，按 `!visible` 设置隐藏状态 | 以可见和隐藏菜单分别验证路由及标签页 |
| 中 | 无权限前端体验 | 403 未统一提示 | 显示统一无权限提示，不清理有效登录态 | 无权限 API 返回 403 时页面提示且会话保留 |
| 低 | 未登录错误页 | 403/404 路由未标记为公开页面 | 标记 `noLogin` | 未登录访问 `/403`、`/404` 不跳转登录页 |

## 待完成的结构性改造

| 等级 | 影响范围 | 根因 | 修复方案 | 验证方式 |
| --- | --- | --- | --- | --- |
| 高 | 密码与账号恢复 | 现有 SHA1、默认密码及历史数据兼容性 | 采用兼容迁移：新增强哈希与密码版本字段，首次成功登录升级旧哈希，并强制默认密码修改 | 新旧账号登录、重置、密码历史和迁移回归测试 |
| 高 | Token 签发 | 签名密钥当前由配置文件提供 | 部署环境注入密钥并轮换现有密钥；启动时拒绝默认或空密钥 | 使用旧密钥签发的 Token 必须失效 |
| 中 | 账号、租户禁用与角色变更 | 会话缓存按设备维度保存，需要统一失效语义 | 引入账号/租户安全版本并在认证、刷新 Token、Hub 连接中校验；变更时递增版本和审计 | 禁用、重置密码、角色变更后各设备 Token 立即失效 |
| 中 | 数据权限与组织范围 | 需逐服务确认查询、详情、导出、批量和统计均叠加租户与组织过滤 | 在仓储层统一叠加租户过滤，再为数据范围提供可组合规范 | 多租户、部门、本人与导出接口集成测试 |

## 数据库升级说明

本次已修复项不修改表结构。密码和会话安全版本改造应采用兼容迁移：先添加可空新列和索引、保留旧哈希验证路径、在成功登录时回填，完成覆盖后再废弃旧列及默认密码逻辑。迁移前必须备份并在生产副本上验证。
