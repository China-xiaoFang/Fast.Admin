# Fast.Admin AI 编码规范

适用于本仓库全部文件。Fast.Admin 是包含后端、Web 和移动端的应用仓库；以业务正确性、跨端合同和可维护性为核心，不把 SDK 的发布规范机械套用到应用代码。

## 工程边界与规则范围

- `Api.Server/`：后端解决方案；相关修改同时读取 `Api.Server/AGENTS.md`。
- `Web.Admin/`：Web 管理端；相关修改同时读取 `Web.Admin/AGENTS.md`。
- `App.Admin/`：uni-app 移动管理端；相关修改同时读取 `App.Admin/AGENTS.md`。
- `App.Client/`：独立的 uni-app 客户端工程；相关修改同时读取 `App.Client/AGENTS.md`。
- `Sql/`、`docs/` 和根资源按现有用途维护；不能仅根据名称把业务或数据来源迁到另一套结构。

从根目录修改子工程时，也应主动读取该子工程的规则。本文件提供共通要求；更具体的目录规则仅作用于对应范围。任务的明确需求可以调整普通风格约定，但不能隐式扩大危险操作授权。

## 工作原则

- 默认用简体中文沟通。简单问题直接给结论；复杂问题说明关键依据、取舍和未确认事项。
- 修改前按影响范围阅读源码、公开类型、配置、调用方、相关测试和文档；不要只凭文件名、历史聊天或示例推断合同。
- 先确认当前分支与工作区修改。保留已有未提交内容；相同文件存在重叠修改时先厘清归属，不回退他人的工作。
- 语言和框架的正式语义优先于历史写法。发现旧实现有误应说明证据并修正相关问题，但不能借“最佳实践”扩大任务。
- 以完成需求所需的合理改动为目标。可以重构有问题的内部实现，不进行无关全仓格式化、目录迁移或依赖升级。
- 跨仓库读取与写入遵循实际授权。共同父目录或同属 Fast 生态，不代表允许修改其他仓库。
- 新增文件放入现有职责目录。只在需求确有需要时引入模块、依赖或抽象；不自动创建总结、计划、架构报告等文件。
- 使用与任务匹配的可用 Skill，但以实际工具链和项目合同为准；不因 Skill 的偏好改换技术栈或自动安装、更新 Skill。

## 需求、业务与接口

- 修改前追踪页面/调用方、接口、服务、模型与权限链路。新增功能明确正常、空数据、加载、失败和无权限等相关状态。
- 不从实体名称、截图或其他 Fast 项目推断业务规则；不确定但会影响数据与权限的决定先明确依据。
- 维持租户、机构、部门、角色和数据范围的真实语义。前端隐藏按钮不等于后端完成授权。
- 接口调整同时核对路径、请求方法、参数、分页、时间与金额单位、枚举、错误和响应包装；不要让前后端各自猜测默认值。
- 只有需求涉及的层才修改；不要为了对齐代码外观改变数据库字段、持久化枚举、业务状态或既有用户流程。
- 跨端共用业务合同不代表共用所有 UI、生命周期和构建实现。Web、App.Admin、App.Client 的角色与平台能力分别确认。
- 重复提交、并发更新、缓存失效和异步通知按真实业务风险处理，不默认引入 MQ、分布式锁或额外服务。

## 结构与依赖

- 沿用现有服务、模型、页面、公共组件和 SDK 扩展点；不并行引入第二套请求、权限、表格、状态或路由体系。
- 先使用 Fast SDK 的公开 API，应用不要引用依赖包私有源码路径或依赖本机相邻仓库才能构建。
- 发现 SDK 缺陷时区分应用适配和 SDK 合同修复；没有跨仓库写授权时报告准确位置，不直接连带重写其他仓库。
- 各子工程分别读取自己的包配置、锁文件、目标框架和生成脚本；不要将 Web 工具链无条件同步到 uni-app 或后端。
- 应用现有的包化 ESLint 接入与部分 SDK 的本地配置不同。基于实际接入修改，不凭旧聊天批量替换模式。
- 不自动移动根工程、重命名子工程或删除相似目录；是否合并工程必须来自明确架构任务。

## 代码、UI 与注释

- 内部代码保持类型严格但简洁；应用页面不需要为每个局部函数编写 SDK 级文档。
- 公共组件、接口类型和共享能力说明必要合同；类型可推断时不过度标注，不使用断言掩盖真实错误。
- 统一处理异常与用户反馈，但不吞掉错误、不重复弹出消息、不遗留 loading。日志携带必要上下文且不得泄露敏感数据。
- UI 延续现有组件、主题、间距、响应式和交互模式。只调整需求涉及的界面，不无故替换 UI 框架或重做全站视觉。
- 注释解释原因、限制和边界；短标签通常不加结束标点，完整陈述正常使用标点。修正过时说明但保留有价值注释。
- 生成声明、接口、路由和页面配置先追踪生成源。不要直接编辑生成结果而不更新输入，也不要误把手写文件当成产物删除。

## 验证与文档

- 在受影响的子工程运行其已有类型、lint、格式和构建检查；测试设施存在且任务适用时执行相关测试。
- 不为普通局部修改顺便搭建整套测试或文档工程。需要新增基础设施时，以明确任务和风险收益为依据。
- 功能变更在任务范围内同步必要的接口说明、使用说明或回归用例；任务明确排除某类文件时记录待同步事项，不把临时排除写成永久规则。
- 编译、H5 构建、小程序开发工具验证、App 真机验证和后端业务验证分别记录，不把一种通过概括为全部通过。
- 启动服务前检查初始化、任务调度、订阅和外部依赖；普通构建授权不包含对真实系统的写入或联调。
- 不伪造根目录统一 `pnpm check` 或 `dotnet test`。具体命令按子工程文件与当前实际配置执行。

## Git 提交说明

- 完成已授权的仓库或包版本更新后，在最终答复中输出可直接复制的推荐 Git 提交说明；有实际文档改动的 Fast.Docs 另给一份，即使文档站自身版本未变。仅提出版本建议、没有实际版本调整时，不得描述成已经更新。
- 根据本任务实际差异、版本变化及 `git status`、暂存/未暂存差异生成说明；区分既有修改，不将不属于本任务的内容混入提交建议。不要为生成说明执行暂存、取消暂存或提交操作。
- 默认使用 `type(scope): 中文摘要`；`feat`、`fix`、`refactor`、`docs`、`chore` 按实际主要改动选择。单纯版本调整使用 `chore(release)`；版本更新伴随功能或修复时，不要只写“更新版本”而省略实质变化。
- 每个独立仓库分别给出一个纯文本代码块，标题为首行；必要的正文与标题之间空一行。仓库名与解释放在代码块外，方便复制到 Git 图形客户端。
- 只描述真实完成的改动。未发布不写“已发布”，未运行验证不写“测试通过”；阻塞或未完成事项在代码块外单独说明。没有有效变更的仓库不生成空提交建议。
- 推荐提交说明不代表执行提交。未经另行明确授权，不执行 `git add`、`commit`、`push`、`tag` 或发布；用户要求命令时再输出供其复制的命令，并按真实差异限定文件范围。

## 安全边界

- 不输出、保存或提交密码、Token、Secret、API Key、私钥及带凭据的地址。示例使用明确占位符；诊断输出须脱敏。
- 不直接执行数据库写入、结构修改、自动迁移或数据初始化；需要时仅生成 SQL 或方案供人工审核，测试环境也不自动建库或写库。
- 未经明确授权，不执行 `git commit`、`push`、`reset --hard`、`clean`、`rebase` 或历史重写，不发布 npm/NuGet 包，不修改远程服务。
- 不使用危险批量删除命令，不覆盖未提交修改。执行构建、测试、安装或工程脚本前，检查其自定义目标和生命周期是否包含发布、危险清理或外部写入。
- 共享 AI 规则与个人会话、缓存、凭据分开管理。不要为了隐藏正式代码或文档变更扩大 `.gitignore`；不擅自修改全局工具配置、Hook、MCP 或权限。

## 交付要求

说明受影响的子工程、业务与接口变化、验证结果、兼容性及未验证项。没有明确要求时不生成一组新的整改报告、设计文档或版本变更。
